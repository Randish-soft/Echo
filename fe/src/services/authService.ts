import apiClient from './apiClient';

export async function loginWithGitHub(): Promise<{username: string; token: string}> {
    // Placeholder logic. Eventually will call a backend endpoint.
    return { username: 'mockUser', token: '12345' };
}
